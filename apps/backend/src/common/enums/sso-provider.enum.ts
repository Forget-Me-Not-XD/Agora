// ========== Imports: ==========
/**
 * SSO Identity providers supported for login
 * GOOGLE - Google OAuth2/OIDC
 * MICROSOFT - Microsoft Identity platform (personal + work/school accounts)
 */

export enum SsoProvider {
    GOOGLE = 'GOOGLE',
    MICROSOFT = 'MICROSOFT',
}