# Training dependencies 
# Install with:  pip install -r requirements-train.txt

# TensorFlow: the full training framework + TFLite converter
# Pin to a specific minor version so scaler.pkl stays compatible.
tensorflow>=2.15.1

# scikit-learn: MinMaxScaler — saves as scaler.pkl for predict.py
scikit-learn>=1.3.0

# numpy: array math throughout train.py
numpy>=1.24.0

# requests: fetches training data from the NestJS API (--api mode)
requests>=2.31.0

# matplotlib: optional — generates training_curves.png for overfitting diagnosis
matplotlib>=3.7.0
